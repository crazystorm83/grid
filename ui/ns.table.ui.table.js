ns.table.ui.table = function (option, datas) {
    this.addClass = function (className) {
        if (this.hasClass(className)) {
            //event 로 데이터 변경
        }
    }

    this.removeClass = function (className) {
        if (this.hasClass(className)) {
            //event 로 데이터 변경
        }
    }

    this.hasClass = function (className) {
        
    }
}