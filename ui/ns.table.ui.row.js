ns.table.ui.row = function (option, options) {
    this.addClass = function (rowId, className) {
        if (this.hasClass(rowId, className)) {
            //event 로 데이터 변경
        }
    }

    this.removeClass = function (rowId, className) {
        if (this.hasClass(rowId, className)) {
            //event 로 데이터 변경
        }
    }

    this.hasClass = function (rowId, className) {
        
    }
}