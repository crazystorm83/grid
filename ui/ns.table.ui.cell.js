ns.table.ui.cell = function (option, datas) {
    this.addClass = function (columnId, rowId, className) {
        if (this.hasClass(columnId, rowId, className)) {
            //event 로 데이터 변경
        }
    }

    this.removeClass = function (columnId, rowId, className) {
        if (this.hasClass(columnId, rowId, className)) {
            //event 로 데이터 변경
        }
    }

    this.hasClass = function (columnId, rowId, className) {
        
    }
}